HalconQRCode sample（仓库说明）
=============================

这是 Galaxy SDK 自带的 Halcon 示例，不属于 Tracking 主链路，也不会被仓库当前的 CMake 自动构建。

当前用途
========

- 验证厂商 SDK 示例是否能独立运行
- 验证 Halcon 与 Galaxy SDK 的联动环境

主工程本身
==========

- 不依赖这个 sample
- 不需要 Halcon 才能构建 `Tracking`

如果你要单独编这个 sample
==========================

需要先安装 Halcon，并保证下面两个环境变量可用：

- `HALCONROOT`
- `HALCONARCH`

然后在当前目录手动执行 `make`。

备注
====

- 如果使用 Halcon 加密狗，可能需要额外权限
- 这个目录保留为厂商样例，不建议把它当成主工程代码路径的一部分
