Galaxy SDK（仓库内集成说明）
==============================

这个目录是当前工程直接使用的大恒 Galaxy SDK 本地副本。

主工程默认通过 `src/camera/CMakeLists.txt` 里的：

```text
GALAXY_SDK_ROOT=src/camera/galaxy_sdk
```

来引用：

- `inc/`
- `lib/x86_64/`
- `config/`

因此，对本仓库的默认构建来说，通常不需要再做系统级安装。

当前目录的主要用途
==================

1. 给 `src/camera` 提供头文件和运行库
2. 提供 USB / GigE 相关配置脚本与规则
3. 保留厂商 PDF、示例和工具程序，便于单机排障

常见子目录
==========

- `inc/`：SDK 头文件
- `lib/x86_64/`：主工程链接的库
- `doc/`：厂商 PDF 文档
- `config/99-galaxy-u3v.rules`：U3V 设备规则
- `SetUSBStack.sh`：USB 栈参数辅助脚本
- `SetSocketBufferSize.sh`：GigE 相关缓冲辅助脚本
- `sample/`：厂商示例

什么时候需要跑 `Galaxy_camera.run`
==================================

只有在你希望把 SDK 安装到系统环境、额外写 udev / 服务配置，或者想单独使用厂商工具时，才需要考虑运行：

```bash
./Galaxy_camera.run
```

对本仓库当前默认构建路径来说，不是必需步骤。

现状说明
========

- 当前项目主要通过 `src/camera` 模块访问这个 SDK
- 常用验证入口是 `tests/build/camera_demo`
- 如果出现设备权限、枚举或链路问题，优先查看：
  - `config/99-galaxy-u3v.rules`
  - `doc/` 下 PDF
  - `SetUSBStack.sh`

备注
====

这个 README 只描述本仓库里的集成方式，不再重复上游完整安装手册。
